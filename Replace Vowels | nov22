// Coded on 17-11-2022

/*
Given a string, write a PAC, pseudocode/algorithm and the subseqent Java code to replace the vowel characters in the input string with the number '1' 
and print the new string. Your code should also print the number of vowels that are replaced in the input string. 
If the input string contains characters other than the alphabetic characters, your code should display : 'invalid input'. 
The input string may contain lower case characters or upper case charaters or both.

For example:

input :

AeFIoT

output:

11F11T

4
*/

import java.util.*;

public class Main{
    public static void main(String args[]){
        Scanner sc = new Scanner(System.in);
        
        String word = sc.nextLine();
        for(int i=0;i<word.length();i++){
            char t = word.charAt(i);
            if(t=='a' || t=='e' || t=='i' || t=='o' || t=='u' || t=='A'||t=='E'||t=='I'||t=='O'||t=='U'){
                System.out.print("~");
            }
            else{
                System.out.print(t);
            }
        }
    }
}
 
